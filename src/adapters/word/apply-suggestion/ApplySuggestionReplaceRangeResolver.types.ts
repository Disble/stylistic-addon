/** Reviewed text pair read from one Word range. */
export interface ApplySuggestionReviewedText {
  /** Current visible reviewed text. */
  current: string;

  /** Original reviewed text still associated with the range. */
  original: string;
}
