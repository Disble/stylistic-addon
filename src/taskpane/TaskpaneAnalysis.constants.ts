/** Abort reason emitted when the backend cannot be reached before analysis starts. */
export const BACKEND_UNAVAILABLE_ABORT_REASON =
  "Backend no disponible. Verifica que el servidor Mastra esté ejecutándose.";
