import type { TaskpaneViewState } from "./TaskpaneViewStore.types";

/** Initial top-level taskpane view shown on first render. */
export const INITIAL_TASKPANE_VIEW_STATE: TaskpaneViewState = {
  view: "main",
};
