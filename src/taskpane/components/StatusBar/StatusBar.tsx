import * as React from "react";

/** Renders the bottom status-bar anchor. */
export function StatusBar(): React.JSX.Element {
  return <div id="status-bar" className="stylistic-status" style={{ display: "none" }} />;
}
