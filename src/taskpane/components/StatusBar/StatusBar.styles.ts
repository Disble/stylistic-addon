import { makeStyles, tokens } from "@fluentui/react-components";

/** Griffel styles for the status bar. */
export const useStatusBarStyles = makeStyles({
  root: {
    flexShrink: 0,
    marginBottom: tokens.spacingVerticalS,
  },
});
