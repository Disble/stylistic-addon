export type ResultsPanelProps = Readonly<Record<string, never>>;

export type ResultsPanelClasses = Readonly<{
  root: string;
  list: string;
  empty: string;
  skeletonList: string;
  skeletonItem: string;
}>;
