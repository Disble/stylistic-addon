/** Props accepted by the results panel wrapper. */
export type ResultsPanelProps = Readonly<Record<string, never>>;

/** Griffel class slots consumed by the results panel. */
export type ResultsPanelClasses = Readonly<{
  root: string;
  list: string;
  empty: string;
}>;
