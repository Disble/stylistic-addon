import { makeStyles, tokens } from "@fluentui/react-components";

/** Griffel styles for the analysis-profile section. */
export const useAnalysisProfileSectionStyles = makeStyles({
  root: {
    marginBottom: tokens.spacingVerticalM,
  },
  field: {
    width: "100%",
  },
  dropdown: {
    width: "100%",
  },
});
